## Start with the right lane

Determina docs are organized by the job you are trying to finish. Start with the hosted workflow when you are reviewing a real behavior change. Use the local lane when you need to verify the package, test an integration shape, or inspect artifacts without platform setup.

<CardGrid cols={2}>
  <Card title="Hosted quickstart" description="Run setup, start your first hosted review, and inspect the results." href="/docs/quickstart" cta="Start" />
  <Card title="Local smoke test" description="Verify the package, config shape, source specs, and existing artifacts without platform setup." href="/docs/local-smoke-test" cta="Run locally" />
  <Card title="Integrations" description="Choose native HTTP, schema-mapped HTTP, Python, agent HTTP session, provider, MCP, or LangGraph-style paths." href="/docs/native-http" cta="Integrate" />
  <Card title="Results Packets" description="Understand reports, traces, manifests, generated coverage sidecars, compare output, and agent artifacts." href="/docs/results-packets" cta="Read results" />
</CardGrid>

<Lane title="Hosted production lane" variant="tip">
Use API credentials with `determina setup` and `determina review run`. Determina groups hosted reviews by commit, branch, release, and deployment metadata when available.
</Lane>

<Lane title="Local package lane" variant="note">
Use local URLs, driver configs, source spec validation, upload previews, and artifact inspection when you are testing setup without platform credentials. Execution workflows still move through hosted platform commands in the public CLI.
</Lane>

## Core model

Every behavior review follows the same nouns:

<DataTable
  headings={["Noun", "What it means"]}
  rows={[
    ["Project", "The hosted container for Systems, runs, artifacts, baselines, and behavior review."],
    ["System", "The AI product surface under review, such as a recommender, search ranker, or agent."],
    ["Review", "A repeatable hosted check that runs a recipe and records what happened."],
    ["Commit / release", "The source metadata Determina uses to group reviews over time when available."],
    ["Trace", "The observed path through inputs, outputs, tool calls, state, timing, and artifacts."],
    ["Finding", "A named behavior risk backed by trace and verifier evidence."],
    ["Results Packet", "The durable artifact set from an audit, compare, generated coverage run, or local review."],
    ["Launch Decision", "A pass, warn, or fail recommendation used by humans during release review."]
  ]}
/>

## Where to go next

If you are integrating a service, start with [Native HTTP](/docs/native-http) or [Schema-Mapped HTTP](/docs/schema-mapped-http). If you are testing agents, read [Agent HTTP Session](/docs/agent-http-session) and [Agent System Type](/docs/agents). If you need explicit platform records for compare or CI, read [Systems and Versions](/docs/systems-and-versions). If you need command details, use the [CLI Reference](/docs/cli-reference).

<CTA title="Run the hosted path first" description="The primary docs path is setup, review run, and results inspection." href="/docs/quickstart" action="Hosted quickstart" />
